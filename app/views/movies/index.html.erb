	<div class="success"> Saved! </div>

	<div id="index">
		<div class="side-contain">
			<aside></aside>
		</div>
	</div>

	<div class="trailer"></div>

	<div class="user-show" id="user-movies">
		<% @movies.each do |movie| %>
			<div data-title="<%= movie.name %>" class="user-show">
				<img src="<%= movie.poster %>">
				<button class="watch-trailer">Trailer</button>

				<a href="https://www.google.com/calendar/render?action=TEMPLATE&text=#{<%= movie.name %>}&dates=#{<%= movie.formatted_date %>}T230807Z/#{<%= movie.formatted_date %>}T003807Z&details=&location=&sprop=&sprop=name:#f" name="addtocalendar" title="Add to Calendar" >
    			Add to Calendar
				</a>
			</div>
		<% end %>


	</div>

	<div class="map-input">
		<div id="map"></div>

		<input type="text" placeholder="20850">
		<button> Find movies </button>
	</div>


<script type="text/javascript" src="https://addthisevent.com/libs/1.5.8/ate.min.js"></script>